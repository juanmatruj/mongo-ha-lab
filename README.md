# mongo-ha-lab

Reproducible MongoDB replica set lab with TLS, RBAC and Ansible-driven provisioning.

## Status

Work in progress.

## Local setup

This repository uses [pre-commit](https://pre-commit.com/) hooks to prevent
committing secrets. After cloning, run:

```bash
pip install pre-commit
pre-commit install
```
